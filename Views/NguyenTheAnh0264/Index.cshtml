@model IEnumerable<NguyenTheAnh0264.Models.NTA0264>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NTAName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NTAGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NTAName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NTAGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NTAId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NTAId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NTAId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
